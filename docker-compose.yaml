services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ENV_TYPE: ${ENV_TYPE:-PRODUCTION}
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ${TSUTMB_HOSTEL_VOLUME:-./resources}:/app/resources
